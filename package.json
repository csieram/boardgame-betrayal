{
  "name": "betrayal-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "turbo": "^1.12.0"
  },
  "packageManager": "pnpm@8.15.0"
}
