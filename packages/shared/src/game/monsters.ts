export interface Monster {
  id: string;
  name: string;
}

export interface TraitorPowers {
  id: string;
  name: string;
}

export const MONSTERS: Monster[] = [];
export const TRAITOR_POWERS: TraitorPowers[] = [];
